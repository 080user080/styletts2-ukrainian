--- app.py
+++ app.py
@@
 import torch
+from pathlib import Path
@@
-single_style = torch.load('filatov.pt')
+def _find_first_existing(paths):
+    for p in paths:
+        pp = Path(p)
+        if pp.exists():
+            return str(pp)
+    return None
+
+# Де може лежати файл стилю Філатова
+_HERE = Path(__file__).parent
+_FILATOV_CANDIDATES = [
+    'filatov.pt',
+    '09_filatov.pt',
+    str(_HERE / 'filatov.pt'),
+    str(_HERE / '09_filatov.pt'),
+    str(_HERE / 'voices' / 'filatov.pt'),
+    str(_HERE / 'voices' / '09_filatov.pt'),
+]
+
+FILATOV_PATH = os.environ.get('FILATOV_PT') or _find_first_existing(_FILATOV_CANDIDATES)
+try:
+    single_style = torch.load(FILATOV_PATH) if FILATOV_PATH else None
+except Exception:
+    single_style = None
+    print("WARN: не вдалося завантажити стиль Філатова:", FILATOV_PATH)
@@
 def synthesize(mode, text, speed, voice_name=None, progress=None):
     """
     mode: 'single' | 'multi'
     """
-    if mode == 'single':
-        # ... використовує single_style
+    if mode == 'single':
+        if single_style is None:
+            raise RuntimeError(
+                "Single-режим недоступний: відсутній файл стилю Філатова.\n"
+                "Задай змінну середовища FILATOV_PT або поклади '09_filatov.pt' у корінь проєкту чи voices/."
+            )
+        # ... використовує single_style як і раніше
         # ...
         pass
