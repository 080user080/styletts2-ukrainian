Ось файл опису контексту проєкту. Ти можеш зберегти його як PROJECT_CONTEXT.md або README_AI.md.
Коли ти починатимеш новий чат зі мною або іншим ШІ, просто скинь цей файл першим повідомленням. Він містить усю необхідну архітектурну логіку, щоб ШІ одразу зрозумів, як писати сумісний код.
# 📂 PROJECT_CONTEXT.md — Архітектурний опис проєкту

## 1. 🎯 Суть Проєкту
Це модульний Python-фреймворк для автоматизації (зокрема, роботи з LLM/Gemini та браузером). 
**Головна філософія:** Максимальна ізоляція модулів, автоматичне збирання конфігурації та виконання у порядку, визначеному числовими префіксами файлів.

Система дозволяє додавати нові функції (наприклад, Telegram-бот, парсер, GUI), просто закинувши **один файл** у папку `kod/`, не змінюючи ядро.

## 2. 🏗️ Архітектура та Правила (MJF - Minimum Viable Framework)

### 🔑 Принцип "Числових префіксів"
Всі модулі лежать у папці `kod/` і мають назву формату: `p_NNN_name.py`.
* **000-099:** Системне ядро (Loader, Config).
* **100-199:** Базові сервіси (Logger, Database).
* **200+:** Бізнес-логіка та функціонал (Translation, Browser Automation).
* **Loader (000)** сканує папку, сортує файли за номером `NNN` і запускає їх по черзі.

### 🧠 Шина даних (`app_context`)
Модулі не імпортують один одного напряму. Вони спілкуються через словник `app_context`, який передається у функцію `initialize()`.
* `app_context['config']` — доступ до налаштувань.
* `app_context['logger']` — доступ до логера.
* Якщо модуль створює сервіс, він кладе його в контекст: `app_context['my_service'] = service_instance`.

### ⚙️ Конфігурація (Distributed Config)
* Немає гігантського файлу налаштувань у коді.
* Кожен модуль сам каже системі, яка структура налаштувань йому треба, через Pydantic-моделі (функція `prepare_config_models`).
* **000_loader** збирає ці моделі, читає `config.yaml` (+ файли з `config/`) і валідує їх.

---

## 3. 📜 Контракт Модуля (API)
Кожен файл `p_NNN_name.py` повинен реалізовувати наступні функції (деякі опційні):

| Функція | Обов'язкова? | Опис |
| :--- | :---: | :--- |
| **`prepare_config_models()`** | Ні | Повертає словник `{'section_name': PydanticModel}`. Додає секцію у глобальний конфіг. |
| **`DEFAULT_CONFIG`** | Ні | Змінна (dict) зі значеннями за замовчуванням. |
| **`check_dependencies()`** | Ні | Перевіряє наявність бібліотек. Повертає `True`/`False` або dict. Loader пропустить модуль, якщо залежностей немає. |
| **`initialize(app_context)`** | **ТАК** | Головна точка входу. Тут модуль запускає свою логіку. |
| **`stop(app_context)`** | Ні | Викликається при завершенні роботи програми (Ctrl+C) для очищення ресурсів. |

---

## 4. 📂 Структура файлів

```text
project_root/
├── main.py                # Тільки запускає kod.p_000_loader.run()
├── config.yaml            # Основні налаштування користувача
├── requirements.txt       # Залежності (PyYAML, pydantic, playwright тощо)
├── config/                # Папка для додаткових конфігів (напр. logging.yaml)
└── kod/                   # Папка з модулями
    ├── p_000_loader.py    # ЯДРО: Сканує модулі, мерджить конфіги, запускає initialize()
    ├── p_001_config.py    # ЯДРО: Валідує зібраний конфіг через Pydantic
    ├── p_100_logger.py    # СЕРВІС: Налаштовує логування (RotatingFile + Console)
    ├── p_302_open_temp_chat.py # ФУНКЦІОНАЛ: Відкриває чат Gemini через Playwright/CDP
    └── ... (інші модулі)

5. 🛠️ Поточний функціонал (Статус)
 * Loader (000): Реалізовано сканування, перевірку залежностей, злиття конфігів та graceful shutdown (stop()).
 * Logging (100): Автономний модуль, підтримує ротацію файлів та кольоровий вивід.
 * Open Temp Chat (302): Використовує Playwright для підключення до Chrome (через CDP port 9222) та відкриття тимчасового чату Gemini.
 * Translation (300): (В планах/частково) Інтеграція з KOD_pereclad для автоматичного перекладу файлів.
6. 🤖 Інструкція для ШІ-розробника
Якщо тебе просять додати новий функціонал:
 * Створи новий файл у kod/ з вільним префіксом (наприклад, p_250_new_feature.py).
 * Визнач Pydantic модель для конфігурації цього модуля.
 * Реалізуй функцію prepare_config_models.
 * Основну логіку пиши в initialize(app_context).
 * Не змінюй p_000_loader.py або main.py, якщо це не критична зміна архітектури.
<!-- end list -->

### Як це допоможе:
Цей файл діє як **"системний промпт"**. Він позбавляє від необхідності щоразу скидати мені весь код. Достатньо скинути цей файл, і я (або інший ШІ) знатиму:
* Що не можна лізти в `main.py` без потреби.
* Що конфігурацію треба описувати через Pydantic.
* Що дані треба брати з `app_context`.

