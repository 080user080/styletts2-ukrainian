--- KOD_pereclad.py
+++ KOD_pereclad.py
@@ -852,7 +852,7 @@
 
 # ===== Entry point =====
 
-def main() -> None:
+def process_all(cfg: dict) -> None:  # Зміна: приймає конфіг замість завантаження
     cfg = load_config(CONFIG_PATH)
     logger.info(f"response_timeout = {cfg.get('response_timeout')}")  
     # Якщо input_folder відсутній - GUI запросить
@@ -861,5 +861,9 @@
     process_all(cfg)
 
 if __name__ == "__main__":
-    main()
+    # Стара точка входу для автономного використання
+    def main() -> None:
+        process_all(load_config(CONFIG_PATH))
+    
+    main()